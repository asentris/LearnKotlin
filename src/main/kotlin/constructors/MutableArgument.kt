package constructors

class MutableArgument(var name:String) {
    //by using var keyword, name becomes a property accessible outside of the object
    //name CAN be changed outside of the object with MutableArgument.name
}