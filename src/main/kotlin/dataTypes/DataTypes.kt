package dataTypes

class DataTypes {
    //Byte = -128 to 127                                                                            [8 bits]
    //Short = -32768 to 32767                                                                       [16 bits]
    //Int = -2^31 to 2^31-1, this is the default number type                                        [32 bits]
    //Long = -2^63 to 2^63-1, if the number is outside Int range, this is the default number type   [64 bits]

    //Float = any number suffixed with 'F', 100F    [32 bits]
    //Double = all floating numbers                 [64 bits]

    //Boolean = true or false [1 bit]
    //Char = a single upper or lower case alphabetic character, 'a' [8 bits]
    //String = one or more characters, "a" / "aa"
}