package loops

//performs the expression once before evaluating the condition
class DoWhileLoops {
    fun doWhileLoop() {
        var x = 10
        do {
            x -= 1
        } while (x > 0)
    }
}
