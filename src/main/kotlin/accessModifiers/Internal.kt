package accessModifiers

internal class Internal {
    //internal modifiers make it so that thing is only accessible within the module it is in
    //modules are typically libraries with multiple packages in them
    //an internal class cannot be accessed outside of the library it is in, but can be within different packages
}